import type { DiffsThemeNames } from '@pierre/diffs';

import themeIds from './ids.json';

/** Theme ids exposed in the diff theme picker. */
export const DIFF_THEME_IDS = themeIds as readonly DiffsThemeNames[];
