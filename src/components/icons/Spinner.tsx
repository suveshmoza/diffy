import { IconDraft, type IconProps } from '@pierre/icons';

/** Loading indicator — pair with `popup-spinner` or `gprv-loading-spinner` for rotation. */
export function IconSpinner(props: IconProps) {
  return <IconDraft {...props} />;
}
